         /*
         Find the minimum number of brackets that we need to remove to make the given bracket sequence balanced. */





        //  psuedo code

        // count is the no of brackets that we need to remove to make the given brackets sequence balanced;
        // count = 0;
        // stack<int> st;

        // for(int i = 0 ; i< n; i++){
        //     if(str[i]== '('){
        //         st.push('(');

        //     }
        //     else{
        //         if(not st.empty()){
        //             st.pop();
        //         }
        //         else{
        //             count++;
        //         }
        //     }
        // }